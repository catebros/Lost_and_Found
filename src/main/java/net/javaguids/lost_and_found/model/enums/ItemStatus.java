package net.javaguids.lost_and_found.model.enums;

// Enum defining the different statuses an item can have in the system
public enum ItemStatus {
    ACTIVE,    // Item is still being searched for
    RESOLVED   // Item successfully reunited with owner
}